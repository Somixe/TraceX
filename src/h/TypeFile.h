#ifndef TYPEFILE_H
#define TYPEFILE_H

/**
 * @brief The TypeFile enum : énumération pour les types de fichiers réprésentés par les noeuds
 *
 * @author malek
 */
enum class TypeFile{
    sss, srs, sdd
};

#endif // TYPEFILE_H
